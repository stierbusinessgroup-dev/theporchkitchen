---
// Decorative background texture removed — the design is intentionally flat and
// monochrome (Yang's-style restraint), letting whitespace and photography carry
// the page. Kept as a no-op so existing `.has-tex` wrappers need no markup change.
---
